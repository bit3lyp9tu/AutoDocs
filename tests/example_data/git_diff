diff --git a/README.md b/README.md
index 460cd93..f384c85 100644
--- a/README.md
+++ b/README.md
@@ -7,5 +7,6 @@ For now, you need to download the renderer manually

 To render a *.puml file manually, run `java -jar resources/plantuml.jar <your_puml_file_path>`.

-
 Valid diagrams are: [https://plantuml.com/guide](https://plantuml.com/guide)
+
+For `tests/configs/commit-convention.md` prompt [https://www.conventionalcommits.org/en/v1.0.0/](https://www.conventionalcommits.org/en/v1.0.0/) was used.
diff --git a/main.py b/main.py
index 9d42751..343dc18 100644
--- a/main.py
+++ b/main.py
@@ -3,6 +3,7 @@ from time import sleep
 from src.config_parser import YAMLConfig
 from src.rendering import PlantUMLRendering
 from src.plantuml_converter import PlantUMLConverter
+from src.request import LLM_API


 def main():
@@ -21,25 +22,38 @@ def main():
         "zai-org/GLM-5.2-FP8"
     ]

+    # source = "tests/example_data/oop.py"
+    # target_path = f"tests/results/benchmarks/uml_class_diagram/"
+    # source = "tests/example_data/db_schema.py"
+    # target_path = f"tests/results/benchmarks/entity_relation_diagram/"
+
+    target_path = "tests/results/benchmarks/commit_msg"
+
     yaml = YAMLConfig('tests/configs/autodocs.yaml')

     for i in range(len(model_list)):
         print(f"Applying Model [{model_list[i]}]...")

-        # source = "tests/example_data/oop.py"
-        # target = f"tests/results/benchmarks/uml_class_diagram/test_{i}.puml"
-        source = "tests/example_data/db_schema.py"
-        target = f"tests/results/benchmarks/entity_relation_diagram/test_{i}.puml"
-
-        PlantUMLConverter(
-            config=yaml.config,
-            model=model_list[i]
-        ).convert(
-            source_file=source,
-            target_path=target
-        )
-
-        PlantUMLRendering(config=yaml.config, render_source_path=target).render()
+        # file_path = target_path + f"test_{i}.puml"
+        # PlantUMLConverter(
+        #     config=yaml.config,
+        #     model=model_list[i]
+        # ).convert(
+        #     source_file=source,
+        #     target_path=file_path
+        # )
+        # PlantUMLRendering(config=yaml.config, render_source_path=file_path).render()
+
+        api = LLM_API(config=yaml.config, model=model_list[i])
+
+        with open(yaml.config.git.commit.sysprompt.file_path, 'r') as r:
+            prompt = '\n'.join(r.readlines())
+
+        result = []
+        try:
+            raw_result = api.call(rule=prompt, prompt=text)
+        except ValueError as err:
+            print(err)

         sleep(yaml.config.llm_service.api.request_delay_seconds)

diff --git a/src/config_model.py b/src/config_model.py
index 585d10e..1a4a989 100644
--- a/src/config_model.py
+++ b/src/config_model.py
@@ -77,12 +77,14 @@ class AutoDocs(BaseModel):
     valid_diagrams: ValidDiagrams
     sysprompt: SysPrompt

+class GitCommit(BaseModel):
+    allow_auto_msg: bool = True
+    sysprompt: SysPrompt

 class Git(BaseModel):
-    pass
-
+    commit: GitCommit

 class Config(BaseModel):
     llm_service: LLMService
     autodocs: AutoDocs
-    # git: Git
+    git: Git
diff --git a/src/config_parser.py b/src/config_parser.py
index 68b8867..ed8bdb0 100644
--- a/src/config_parser.py
+++ b/src/config_parser.py
@@ -3,7 +3,7 @@ import yaml
 from src.config_model import Config


-class YAMLConfig():
+class YAMLConfig:
     def __init__(self, config_path="autodocs.yaml") -> None:
         self.config_path = config_path
         with open(self.config_path, 'r') as f:
diff --git a/src/git_master.py b/src/git_master.py
new file mode 100644
index 0000000..9b4e222
--- /dev/null
+++ b/src/git_master.py
@@ -0,0 +1,11 @@
+
+from src.config_model import Config
+
+
+class GitMaster:
+    def __init__(self, config: Config) -> None:
+        self.config = config
+
+    def diff(self):
+        # git diff HEAD~1
+        pass
diff --git a/src/request.py b/src/request.py
index c8ec31d..430fbae 100644
--- a/src/request.py
+++ b/src/request.py
@@ -6,11 +6,13 @@ from src.config_model import Config


 class LLM_API:
-    def __init__(self, config: Config, llm_key_path="~/.llm_key", model="meta-llama/Llama-3.3-70B-Instruct") -> None:
+    def __init__(self, config: Config, model="meta-llama/Llama-3.3-70B-Instruct") -> None:
         self.base_url = config.llm_service.api.base_url

         if not config.llm_service.api.key_value:
-            path = Path(llm_key_path).expanduser()
+            key_location = str(config.llm_service.api.key_location)
+
+            path = Path(key_location).expanduser()
             with path.open("r", encoding="utf-8") as f:
                 self.llm_key = f.read().strip()
         else:
diff --git a/tests/configs/autodocs.yaml b/tests/configs/autodocs.yaml
index 5fc12f2..55b53c9 100644
--- a/tests/configs/autodocs.yaml
+++ b/tests/configs/autodocs.yaml
@@ -33,4 +33,8 @@ autodocs:
   sysprompt:
     file_path: 'tests/configs/autodocs-prompt.md'
     # content: ''
-# git:
+git:
+  commit:
+    allow_auto_msg: True
+    sysprompt:
+      file_path: 'tests/configs/commit-convention.md'

